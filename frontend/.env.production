# Production Environment Variables  
# Used when building for production with npm run build

# Backend Configuration - Will auto-detect if not specified
# VITE_BACKEND_URL=  # Leave empty for auto-detection

# External Services
VITE_RAZORPAY_KEY_ID=your_razorpay_live_key
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Production Settings
VITE_ENVIRONMENT=production
VITE_DEBUG=false